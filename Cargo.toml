[workspace]
members = [
    "rust-create-agent",
    "rust-agent-middlewares",
    "rust-agent-tui",
    "rust-relay-server",
    "langfuse-client",
]
resolver = "2"

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
